= render 'expense/options'
%table
  %tr
    %th Expense number
    %th Filename
  - @expenses.each do |expense|
    %tr
      %td= expense.expense_number
      %td= link_to "#{expense.filename}", :controller => 'expense', :action => 'download' , :id => expense.id